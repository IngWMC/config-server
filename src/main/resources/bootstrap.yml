server:
  port: 7075
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IngWMC/config-data.git
          #searchPaths: config-data
          username: ingwmc
          password: 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
          
  # Credenciales para que se conecten al config-server
  security:
    user:
      name: root
      password: s3cr3t0